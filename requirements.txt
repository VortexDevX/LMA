# ============================================================
# LOCAL MONITORING AGENT — DEPENDENCIES
# ============================================================

# --- Core ---
psutil>=5.9.0
requests>=2.31.0

# --- System Tray ---
pystray>=0.19.0
Pillow>=10.0.0

# --- DNS / Network (optional, for domain sniffing) ---
scapy>=2.5.0
dnspython>=2.4.0

# --- macOS specific (installed conditionally) ---
# pyobjc-core>=10.0 (macOS only)
# pyobjc-framework-Cocoa>=10.0 (macOS only)
# pyobjc-framework-Quartz>=10.0 (macOS only)

# --- Build & Packaging ---
pyinstaller>=6.0.0

# --- Testing ---
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
responses>=0.23.0

# --- Linting & Formatting ---
ruff>=0.1.0
black>=23.0.0

# --- Scheduling ---
schedule>=1.2.0
